name: キャラクター自動生成

on:
  # 手動実行のみ
  workflow_dispatch:
    inputs:
      custom_prompt:
        description: 'カスタムプロンプト（任意）: CSV内容を修正するための追加指示'
        required: false
        type: string
        default: ''

jobs:
  generate-character:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    defaults:
      run:
        working-directory: WorkFlow_origin

    steps:
      # リポジトリのチェックアウト
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      # Node.jsのセットアップ
      - name: Node.jsをセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'WorkFlow_origin/package.json'

      # 依存関係のインストール
      - name: 依存関係をインストール
        run: npm ci

      # キャラクターCSV生成
      - name: キャラクターCSVを生成
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          CUSTOM_PROMPT: ${{ github.event.inputs.custom_prompt }}
        run: npm run generate-character

      # 生成されたCSVをArtifactとしてアップロード
      - name: 生成されたCSVをアップロード
        uses: actions/upload-artifact@v4
        with:
          name: character-csvs
          path: WorkFlow_origin/character/*/*.csv
          retention-days: 90

      # 結果をコミット
      - name: 結果をコミット
        if: success()
        working-directory: .
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          # 変更があればコミット
          if [[ -n $(git status -s WorkFlow_origin/character/) ]]; then
            git add WorkFlow_origin/character/
            git commit -m "🤖 自動生成: キャラクターCSV $(date '+%Y-%m-%d')"
            git push
          else
            echo "変更がないため、コミットをスキップします"
          fi

      # 完了メッセージ
      - name: 完了
        run: |
          echo "✅ キャラクターCSV生成完了"
          echo ""
          echo "📦 Artifactからダウンロード可能:"
          echo "   character-csvs - 生成されたキャラクターCSVファイル"
